VITE_API_BASE_URL=http://localhost:8080/api/v1

# Free-tier error tracking (https://sentry.io) - leave blank to disable entirely (default).
VITE_SENTRY_DSN=

# Stripe test-mode publishable key (https://dashboard.stripe.com/test/apikeys) - leave blank to disable the payments checkout UI entirely (default).
VITE_STRIPE_PUBLISHABLE_KEY=
